<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- SLA Form View -->
    <record id="view_helpdesk_sla_form" model="ir.ui.view">
        <field name="name">helpdesk.sla.form</field>
        <field name="model">helpdesk.sla</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object" class="oe_stat_button" icon="fa-archive">
                            <field name="active" widget="boolean_button" options="{'terminology': 'archive'}"/>
                        </button>
                    </div>
                    <group>
                        <group>
                            <field name="name" placeholder="e.g. Urgent Response - 4 hours"/>
                            <field name="team_id"/>
                            <field name="priority"/>
                        </group>
                        <group>
                            <field name="response_time" widget="float_time"/>
                            <field name="resolution_time" widget="float_time"/>
                            <field name="warning_threshold"/>
                            <field name="sequence"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- SLA Tree View -->
    <record id="view_helpdesk_sla_tree" model="ir.ui.view">
        <field name="name">helpdesk.sla.tree</field>
        <field name="model">helpdesk.sla</field>
        <field name="arch" type="xml">
            <tree>
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="team_id"/>
                <field name="priority"/>
                <field name="response_time" widget="float_time"/>
                <field name="resolution_time" widget="float_time"/>
                <field name="active"/>
            </tree>
        </field>
    </record>

    <!-- SLA Action -->
    <record id="action_helpdesk_sla" model="ir.actions.act_window">
        <field name="name">SLA Policies</field>
        <field name="res_model">helpdesk.sla</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>